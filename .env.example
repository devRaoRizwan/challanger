# Set these in Vercel → Project → Settings → Environment Variables.
# Use 6+ characters; each PIN lets that person tick their own boxes.
RAO_PIN=
ANEEQ_PIN=

# Added automatically when you connect Upstash Redis from the Vercel Storage tab.
KV_REST_API_URL=
KV_REST_API_TOKEN=
